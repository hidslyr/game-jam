I want to update the pipe

Task 1. Now it doesn't need to swap material follow basket color
Task 2. I want the pipe has two parts.
    - First part is the current one.
    - Second part will be the frame go through all the pieces, this will never drop, just hang in its position.
    - Second part contains multiple pipe segments, each segment is from piece to piece in order.
    - Remove the target destination change of the pipe when a piece is cleared.
    - Now when a piece is cleared, a pipe segment of that piece will be added to the first part (visually, just need the air pumping animation go through it).
    - The second part must be stable all the time, to make player can see the frame go through all the pieces, like a skeleton.